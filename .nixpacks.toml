[phases.setup]
nixPkgs = ["php82", "php82Extensions.pcntl"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php artisan reverb:start --host=0.0.0.0 --port=$PORT"